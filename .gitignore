__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.env
# git worktrees created at runtime for test-takers
.worktrees/
# Phoenix local trace store
.phoenix/
# graphify local-only artifacts (the graph itself is committed)
graphify-out/cache/
graphify-out/cost.json
# archived eval runs (JSON), browsable in the dashboard History mode
runs/
